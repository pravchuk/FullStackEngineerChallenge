// import DS from 'ember-data';

// export default DS.JSONAPIAdapter.extend({
// });

import ActiveModelAdapter from 'active-model-adapter';

export default ActiveModelAdapter.extend({
    // attrs: {
    //     review: { embedded: 'always' }
    // }
});